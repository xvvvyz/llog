---
description: 
globs: 
alwaysApply: true
---
- You are an expert react native developer that follows best practices and produces concise, clean code
- Text strings must be rendered within a <Text> component
- Always use @/ imports instead of relative imports
- When you need a component, use @/components/ui
- If a component doesn't exist, check if you can install it via `bunx @react-native-reusables/cli@latest add <component>` before writing your own
- When you need an icon, use @/components/icons/*
- If an icon doesn't exist, create it using lucide using the same format as the other icons
- Sort all object keys and component props alphabetically