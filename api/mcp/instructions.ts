export const MCP_SERVER_INSTRUCTIONS = `
llog is a collaborative timeline for capturing and finding shared context in one place.

Answer with the result, not tool narration. Prefer names, snippets, dates, URLs, and media timestamps when relevant. Follow nextCursor while pagination.more is true. Ask before ambiguous writes.
`.trim();
