import { UI } from '@/theme/ui';

export const RIPPLE = Object.freeze({
  light: {
    default: UI.light.ripple,
    inverse: UI.light.rippleInverse,
  },
  dark: {
    default: UI.dark.ripple,
    inverse: UI.dark.rippleInverse,
  },
});
